apiVersion: apps/v1
kind: Deployment
metadata:
  name: kgu-opensw
  namespace: cicd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kgu-opensw
  template:
    metadata:
      labels:
        app: kgu-opensw
      annotations:
        buildTime: "20241202-101010"
    spec:
      containers:
      - name: tomcat
        image: juho8345/kgu-opensw:latest
        ports:
        - containerPort: 8080
